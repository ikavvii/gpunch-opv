# GPunch Android APK

Download `GPunch-v1.0.0.apk` from this folder to install the GPunch Android app.

This build is configured with the public GitHub Codespaces backend:

`https://animated-memory-444rr9rrgpwf6rr-3000.app.github.dev/`

On Android, allow installation from unknown sources when prompted.
